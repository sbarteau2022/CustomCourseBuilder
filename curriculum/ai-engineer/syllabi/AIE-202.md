# AIE-202 — Deep Learning: Autograd to Transformers

**Tier:** core · **Program weight:** 9% · **Hours:** 150 · **Durability:** durable · **Pace:** 10–14 hrs/wk

**Prerequisites:** AIE-201
**Co-requisites:** AIE-203

## The descent arc

Descend to raw arrays and build an autograd engine — the chain rule you derived in AIE-103, now as a machine you wrote. Build up through CNN, RNN, transformer, each earned from scratch. Re-ascend fluent in PyTorch, wielding it as an instrument whose every abstraction you could rebuild.

## What outpaces the field

The Karpathy build-it-yourself ethos with what Zero-to-Hero lacks: assessment, ablation discipline, debugging drills on deliberately broken training runs, and a straight line into the LLM and production courses. Every architecture is earned by building, then made fluent in PyTorch.

## Outcomes — what the learner can DO

- Build a working autograd engine and a training loop from raw arrays; explain every line
- Train CNNs, RNNs, and a transformer from scratch on real data, debugging instability from loss curves
- Implement attention and the full transformer block from the paper, then in PyTorch idiom
- Ablate a model systematically and attribute performance to components with evidence

## Assessment (weights sum to 100)

| Component | % | What it measures |
|---|---|---|
| build milestones | 40 | autograd → MLP → CNN → transformer, each verified against reference |
| broken-run clinics | 25 | diagnose planted training pathologies from artifacts alone |
| ablation project | 25 | a written ablation study with defensible attribution |
| sealed-readings | 10 | weekly three-tier readings |

## Modules — each is a dispatchable work packet (see DISPATCH.md)

| Packet | Module | Hours |
|---|---|---|
| `AIE-202-M01` | Autograd from scratch: the tape and the chain rule | 18 |
| `AIE-202-M02` | Training dynamics: init, normalization, optimizers, schedules | 20 |
| `AIE-202-M03` | CNNs and representation learning | 18 |
| `AIE-202-M04` | Sequences: RNNs to attention | 18 |
| `AIE-202-M05` | The transformer, from the paper | 22 |
| `AIE-202-M06` | PyTorch fluency and the modern stack | 18 |
| `AIE-202-M07` | Debugging training: the pathology clinic | 20 |
| `AIE-202-M08` | Ablation and attribution | 16 |

## Ethics thread (the three-tier reading practice for this subject)

Tier 1: scaling results replicate; interpretability lags capability, documented by the field itself. Tier 2: what 'we understand the architecture' suppresses — architecture is not behavior; builders ship systems whose specific capabilities surprise them. Tier 3: where does your responsibility end — at what you built, or at what it learned?

---

**Material-design status:** SCAFFOLD. Each module packet above awaits full lesson-level
authoring per the DISPATCH.md contract: lesson notes, labs (starter + solution + tests),
the module assessment with rubric, Elle/instructor pacing notes, and the module's
three-tier reading prompt.

<!-- BELOW THIS LINE: hand-authored / agent-authored detail, preserved on regen -->

