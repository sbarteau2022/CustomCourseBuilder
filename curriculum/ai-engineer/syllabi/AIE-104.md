# AIE-104 — The Machine at Scale: Linux, Networks, Concurrency, Containers

**Tier:** foundation · **Program weight:** 4% · **Hours:** 70 · **Durability:** durable · **Pace:** 6–10 hrs/wk

**Prerequisites:** AIE-102


## The descent arc

Descend from 'the server handles it' to the socket, the process, the shared byte two threads fight over. Build a service from those parts. Re-ascend able to reason about a running system's failure modes instead of trusting its abstractions.

## What outpaces the field

The operational half of the machine, taught right after the descent so concurrency lands on top of real memory understanding, not before it. Every concept is exercised on real systems; the container is opened up, not invoked.

## Outcomes — what the learner can DO

- Use Linux fluently: shell, processes, filesystems, permissions, signals
- Reason about networking and APIs from the wire up: sockets, HTTP, TLS, latency
- Reason about concurrency and parallelism well enough to not corrupt shared state — and know why the GIL from AIE-102 forces process-level parallelism in Python
- Containerize a service reproducibly and know what the container actually is

## Assessment (weights sum to 100)

| Component | % | What it measures |
|---|---|---|
| systems labs | 50 | shell, socket, and concurrency programs on real machines |
| estimation exams | 20 | predict latency/throughput/memory before running; graded on calibration |
| service project | 20 | containerized service with a measured resource envelope |
| sealed-readings | 10 | weekly three-tier readings |

## Modules — each is a dispatchable work packet (see DISPATCH.md)

| Packet | Module | Hours |
|---|---|---|
| `AIE-104-M01` | Linux, the shell, processes, and signals | 14 |
| `AIE-104-M02` | Networking and APIs from the wire up | 16 |
| `AIE-104-M03` | Concurrency, parallelism, and shared state | 16 |
| `AIE-104-M04` | Containers and reproducible environments, opened up | 14 |
| `AIE-104-M05` | Putting it together: a measured, containerized service | 10 |

## Ethics thread (the three-tier reading practice for this subject)

Tier 1: the systems that scale are the systems designed to operate without knowing their users individually. Tier 2: what 'scale' suppresses — scale is a decision about relationship, not just throughput. Tier 3: the service you just built holds strangers' data; what do you owe them that no container enforces?

---

**Material-design status:** SCAFFOLD. Each module packet above awaits full lesson-level
authoring per the DISPATCH.md contract: lesson notes, labs (starter + solution + tests),
the module assessment with rubric, Elle/instructor pacing notes, and the module's
three-tier reading prompt.

<!-- BELOW THIS LINE: hand-authored / agent-authored detail, preserved on regen -->

